## RENAMED Requirements

### Requirement: Spec folder rename
FROM: `workload-lifecycle-hooks`
TO: `kit-lifecycle-hooks`

All occurrences of "workload" in this spec's requirements, scenarios, and prose are replaced with "kit".
Key substitutions: "workload" → "kit", `config.yaml` → `kit.yaml`, `WorkloadHookExecutor` → `KitHookExecutor`, `Event.Workload.HookFailed` → `Event.Kit.HookFailed`.
