## RENAMED Requirements

### Requirement: Spec folder rename
FROM: `workload-metrics-declaration`
TO: `kit-metrics-declaration`

All occurrences of "workload" in this spec's requirements, scenarios, and prose are replaced with "kit".
Key substitutions: "workload" → "kit", `config.yaml` → `kit.yaml`, `WorkloadMetrics` → `KitMetrics`, ConfigMap name pattern `easydblab-metrics-<workload>` → `easydblab-metrics-<kit>`.
