## RENAMED Requirements

### Requirement: Spec folder rename
FROM: `workload-running-state`
TO: `kit-running-state`

All occurrences of "workload" in this spec's requirements, scenarios, and prose are replaced with "kit".
Key substitutions: `runningWorkloads` → `runningKits`, `__RUNNING_WORKLOADS__` → `__RUNNING_KITS__`, env var `RUNNING_WORKLOADS` → `RUNNING_KITS`.
