## RENAMED Requirements

### Requirement: Spec folder rename
FROM: `uninstalled-workload-hint`
TO: `uninstalled-kit-hint`

All occurrences of "workload" in this spec's requirements, scenarios, and prose are replaced with "kit".
Key substitutions: "workload" → "kit", hint message updated to: `<name> is not installed. Run: easy-db-lab install <name>` (no change to message text needed — it references the kit name, not the word "workload").
