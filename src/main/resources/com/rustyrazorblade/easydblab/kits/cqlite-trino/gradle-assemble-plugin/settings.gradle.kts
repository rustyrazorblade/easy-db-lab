// Throwaway Gradle project: no source of its own, exists solely to resolve
// the published in.mcfad:cqlite-trino artifact from Maven Central and Sync
// its runtime classpath into a Trino plugin directory. See build.gradle.kts.
rootProject.name = "cqlite-trino-plugin-assembly"
